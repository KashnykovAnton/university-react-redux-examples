const initialState = { items: [] };

// export const itemsReducer = (state = { items: [] }, { payload, type }) => {
//       return state;
// };

export const itemsReducer = (state, { payload, type }) => {
  return initialState;
};
